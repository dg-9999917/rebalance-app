# 구현 진행 상황

## 완료된 단계
- [x] ① git 초기화 + 기본 골격(탭 전환·헤더 버튼) + 상태 객체 + localStorage 저장/백업/불러오기/초기화
- [x] ② 기준설정 탭: P0·fx0·fxN 입력, 설정비율 ±/직접, 6/12종가 입력, 배정금액·비중1당주식수·6/12보유수량 자동산출, 비율합계 검증, 종목 추가/삭제
- [x] ③ 포트폴리오 탭: 입력(현재가·평단·수량) 즉시 계산(평가·현재비중·손익) + 조정비율 ±→필요매매 미리보기 + 시장종합 + 요약 바
- [x] ④ 확정 저장: 실제 체결 입력(체결수량·단가) → 장부 갱신(보유·평단·unit·실현손익) + 이력 스냅샷
- [x] ⑤ 이력 탭: 확정 스냅샷 목록·상세 보기·이 시점으로 복원·되돌리기(직전 확정 취소)
- [x] ⑥ 헤더 겹침 수정(sticky z-index 20 + box-shadow), 금액/가격 입력 천단위 콤마, 수량 소수 2자리, 포커스 시 콤마 제거·블러 시 재포맷

## 완료된 추가 개선 (포트폴리오 탭 polish)
- [x] ⑦ fxN 미입력 시 미국 종목 오계산 방지: priceKRW/avgKRW `|| 1` → `|| 0`, 현재비중·평가손익 '—' 표시, 미국 합계행 '환율 미입력' 경고, 요약 바 '(미국 제외)' 표기
- [x] ⑧ 조정비율 합계 실시간 표시: 요약 바에 추가, 100=초록 / 불일치=빨강 색상
- [x] ⑨ 필요매매 숫자 천단위 콤마: fmtDec → fmtNum(need,2) (현금 대금액 가독성)
- [x] ⑩ 설정비율·조정비율 열 정렬: ratio-cell justify-content flex-end→center, th class num→center
- [x] ⑪ [미리보기] 버튼 라벨: '미리보기 (리밸런싱 시뮬)' → '미리보기 (Beta)'
- [x] ⑫ 포트폴리오 요약 바의 '현재 환율'을 직접 수정 가능한 입력칸으로 전환 (기준설정 fxN과 양방향 동기화, oninput 중 커서 유지)
- [x] ⑬ 이력 탭 — 거래로 보유수량이 변한 종목 행 색 강조 (매수=파랑, 매도=빨강 tint) + 종목명에 ▲매수/▼매도 배지 + 보유 변화량 표시
- [x] ⑭ 이력 탭 — '비중1당' 열 추가 (직전 스냅샷 대비 unit 증감 표시)
- [x] ⑮ 현금 행 표시 정리: 포트폴리오·이력 탭 모두 현재가·평단가·1%당주식수 → '—' (연산 로직은 유지)

## 현재 진행 중
- 없음

## 남은 단계
- 없음 (전 단계 완료)

## 미해결 이슈
- 확정 저장 시 "행 추가"(시뮬 외 추가 수동 체결) 기능 미구현 — 필요 시 후속 추가
- 이력 탭 "두 시점 비교" 미구현 — 필요 시 후속 추가

## 메모
- 저장 키: `rebalance_state_v1` (localStorage)
- 파생값(평가·현재비중·필요매매·시장종합)은 매번 계산, 저장 안 함
- `setPrice(id,value)` / `setBasePrice(id,value)` — 자동화 대비 분리된 가격 설정 함수
- 최초 확정 시 "초기 상태" 스냅샷을 history 끝에 push → 되돌리기 기준점으로 사용
- 되돌리기: history[0] 제거 후 새 history[0]으로 state 복원 (history < 2면 불가)
